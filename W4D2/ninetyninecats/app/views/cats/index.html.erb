<ul>
  <% @cats.each do |cat| %>
    <li><%= cat.name %></li>
    <span><%= cat.description %></span>
    <!-- <a href = "<%#= cat_url(cat) %>">Details</a> -->
    <%= link_to "#{cat.name} details", cat_url(cat) %>
  <% end  %>
</ul>
