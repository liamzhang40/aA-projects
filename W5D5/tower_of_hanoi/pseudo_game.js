class Game {
  run() {
    // until all the disks are on array[1] or array[2] (either one having a length of 3),
    // keep asking for user input.
    // move disks based on from_tower and to_tower input.
  }
}
